# 🎛️ لوحة التحكم - نظام إدارة أسعار الصرف

<div dir="rtl">

مشروع لوحة تحكم احترافية لإدارة أسعار الصرف، مبني باستخدام Next.js 14 و TypeScript و Tailwind CSS.

</div>

---

## 🚀 البدء السريع

### التثبيت

```bash
npm install
```

### التشغيل

```bash
npm run dev
```

افتح [http://localhost:3000](http://localhost:3000) في المتصفح.

---

## 📁 هيكلية المشروع

```
control-panel/
├── app/                    # Next.js App Router
│   ├── layout.tsx         # التخطيط العام
│   ├── page.tsx           # الصفحة الرئيسية
│   └── globals.css        # التنسيقات العامة
│
├── components/            # مكونات React
│   ├── ui/               # مكونات UI صغيرة
│   ├── layout/           # مكونات التخطيط
│   └── sections/         # أقسام الصفحة
│
├── data/                 # البيانات المنفصلة
│   ├── navigation.ts
│   ├── exchange-rates.ts
│   └── dashboard-stats.ts
│
└── lib/                  # الأدوات المساعدة
    ├── utils.ts
    ├── types.ts
    └── constants.ts
```

---

## 🛠️ التقنيات المستخدمة

- **Next.js 14** - إطار عمل React
- **TypeScript** - للسلامة النوعية
- **Tailwind CSS** - للتصميم
- **React 18** - مكتبة UI

---

## 📝 المكونات الرئيسية

### Layout
- `Sidebar` - الشريط الجانبي
- `Header` - رأس الصفحة

### UI Components
- `StatCard` - بطاقة إحصائية
- `StatusBadge` - شارة الحالة
- `TableCell` - خلية الجدول
- `FilterSelect` - قائمة الفلترة
- `SearchInput` - حقل البحث
- `Pagination` - التصفح بين الصفحات

### Sections
- `DashboardStatsSection` - قسم الإحصائيات
- `FiltersSection` - قسم الفلاتر
- `ExchangeRatesTable` - جدول أسعار الصرف

---

## 🎨 الألوان

جميع الألوان محددة في `tailwind.config.ts`:

- **Primary**: `#334090`
- **Background**: `#ffffff`, `#f5f8f7`, `#f9f9f9`
- **Status Active**: `#4caf50`
- **Status Stopped**: `#dc2626`

**ملاحظة**: استخدم الألوان من config وليس hex codes مباشرة.

---

## 🔧 الأوامر المتاحة

```bash
npm run dev      # تشغيل المشروع
npm run build    # بناء المشروع
npm run start    # تشغيل الإنتاج
npm run lint     # فحص الكود
```

---

## 📋 قواعد مهمة

1. ✅ استخدم TypeScript دائماً
2. ✅ فصل البيانات في مجلد `data/`
3. ✅ استخدم الألوان من `tailwind.config.ts`
4. ✅ استخدم `cn()` من `lib/utils.ts` لدمج classes
5. ❌ لا تستخدم hex codes مباشرة
6. ❌ لا تكتب البيانات في JSX

---


## 📄 الرخصة

 بشركة اسبيد هذا المشروع خاص.

---

**آخر تحديث**: يناير 2026
